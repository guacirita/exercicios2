/**8. Desenvolva uma função que calcule a área de um círculo. 
 * A função deve receber o raio como parâmetro e retornar a área calculada. */

function areaCirculo(raio) {
  raio = 2.5;
  area = 2 * Math.PI * raio;

  return area 
}

console.log(areaCirculo());